{
  "name": "@ohcnetwork/leaderboard-slack-plugin",
  "version": "0.1.0",
  "private": false,
  "description": "Leaderboard scraper to track EOD messages from a slack channel.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --packages=external",
    "test": "vitest run",
    "test:watch": "vitest",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "keywords": [
    "leaderboard",
    "plugin"
  ],
  "packageManager": "pnpm@10.20.0",
  "dependencies": {
    "@ohcnetwork/leaderboard-api": "^0.4.0",
    "@slack/web-api": "^7.19.0",
    "date-fns": "^4.4.0",
    "slack-markdown": "^0.3.0",
    "tsx": "^4.23.12"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/node": "^20.19.43",
    "esbuild": "^0.27.7",
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  },
  "author": "Open Healthcare Network",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ohcnetwork/leaderboard-slack-plugin.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  }
}
